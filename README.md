# i.emkacz.dev URL Shortener

Self-hosted production-grade URL shortener consisting of a Fastify + Prisma + PostgreSQL backend and a Vite + React + Tailwind frontend. Deployable via Docker Compose and Nginx Proxy Manager with SSL.

